# TODO

## Before Phase 4

### Manual verification (requires `make dev` for proxied providers)

- [x] Sidebar UI — open/close, all three tabs render, map fills width when collapsed
- [ ] "Global Summer" → Mapbox outdoors base, no topo overlay
- [ ] "Switzerland" → swisstopo topo overlay loads over outdoors base (proxied, blank tiles → transparent PNG)
- [ ] "Switzerland Winter" → grayscale swisstopo loads, ski touring/snowshoe toggles appear in Overlays tab
- [ ] "France" → IGN topo overlay loads (proxied, needs IGN_API_KEY)
- [ ] "Austria" → basemap.at topo overlay loads (direct)
- [ ] "Germany" → BKG TopPlusOpen topo overlay loads (direct)
- [ ] "Norway" → Kartverket topo overlay loads (direct)
- [ ] "United States" → USGS topo overlay loads (direct)
- [ ] Switch from country topo back to "Global Summer" → topo overlay removed
- [ ] "Satellite Summer" → satellite base layer loads
- [ ] "Global Winter" / "Satellite Winter" cards render as disabled with "Coming soon"
- [ ] Toggle overlays on/off — layers appear/disappear on map
- [ ] 2D/3D map control — terrain enables with pitch tilt, disables back to flat
- [ ] Custom exaggeration in Settings → slider appears, adjusts terrain exaggeration in real-time
- [ ] Flat/globe projection in Settings (Firefox private browsing disables terrain due to Canvas2D fingerprinting protection)

### Automated

- [x] `go test ./internal/tiles/...` with Redis — runs in CI (test-api job)

## Soon

- [ ] Web bundle size is large (MapPage chunk ~1.7MB) — plan code-splitting before phases add more UI/features
- [ ] Add tile-proxy observability (cache hit ratio, upstream error rate, per-provider request volume)
- [ ] Sentry for error tracking (free tier)
- [ ] Mapbox token URL restrictions (lock pk.* token to production domain)
- [ ] Logger middleware drops optional http.ResponseWriter interfaces (Flush, Hijack, Push) — fix before adding SSE/WebSocket/streaming features
- [ ] Add integration test for duplicate-email 409 path (requires real Postgres)
- [ ] Capability-aware basemap cards (hide IGN/Sentinel when backend env vars not configured)
- [ ] BKG TopPlusOpen serves global tiles (tested: Madrid, Rome, London, Moscow, New York all return data) — consider expanding bbox or offering it as a global topo alternative
- [ ] Create custom Mapbox Studio winter style for Global Winter + Satellite Winter basemap cards (muted/cool tones, desaturated greens, blue-gray terrain)
