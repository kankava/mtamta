# TODO

## Before Phase 4

- [ ] Manual: verify sidebar UI — open/close, all three tabs render, map fills width when collapsed
- [ ] Manual: click each basemap card — map style, topo overlay, and season all change atomically
- [ ] Manual: select country topo card (e.g. swisstopo) — topo overlay loads; switch back to Outdoors Summer — topo overlay removed
- [ ] Manual: select "swisstopo Winter" → winter topo tiles load, ski touring/snowshoe toggles appear in Overlays tab
- [ ] Manual: select "Satellite Summer" → satellite base layer loads (Satellite Winter card is disabled/coming soon)
- [ ] Manual: toggle overlays on/off — layers appear/disappear on map
- [ ] Manual: click 2D/3D map control — terrain enables with pitch tilt, disables back to flat
- [ ] Manual: toggle custom exaggeration in Settings → slider appears, adjusts terrain exaggeration in real-time
- [ ] Manual: toggle flat/globe projection in Settings (note: Firefox private browsing disables terrain due to Canvas2D fingerprinting protection)
- [ ] Manual: proxy tiles load through `/api/v1/tiles/opentopomap/...` with Redis caching (cache HIT/MISS headers)
- [ ] Run full `go test ./internal/tiles/...` locally with Redis/network enabled
- [ ] Add one lightweight e2e smoke test for map behavior (basemap card switching + sentinel request)
- [ ] Decide if in-memory tile rate limiter is enough (single instance = fine; multi-instance = move to shared limiting)

## Soon

- [ ] Web bundle size is large (MapPage chunk ~1.7MB) — plan code-splitting before phases add more UI/features
- [ ] Add tile-proxy observability (cache hit ratio, upstream error rate, per-provider request volume)
- [ ] Sentry for error tracking (free tier)
- [ ] Mapbox token URL restrictions (lock pk.* token to production domain)
- [ ] Logger middleware drops optional http.ResponseWriter interfaces (Flush, Hijack, Push) — fix before adding SSE/WebSocket/streaming features
- [ ] Add integration test for duplicate-email 409 path (requires real Postgres)
- [ ] Capability-aware basemap cards (hide IGN/Sentinel options when backend env vars not configured)
